**lint-suppression** — `@ts-ignore` / `# noqa` sem código de regra não resolve o
problema: esconde o sensor. É o anti-hábito número um — a métrica passa e o
defeito fica. Corrija a causa; se a supressão é genuinamente necessária, use a
forma ESTREITA (regra específica, uma linha) com comentário do porquê.
