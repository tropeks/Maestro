# Estado local do Maestro (logs e decision records, efêmeros e com metadados de
# projeto; DATA_MODEL §3/§4) mora em ~/.maestro, fora do repo. O `.maestro/` do
# PROJETO carrega artefato que viaja com o repo: INTENT.md (E22) e orders/ (E15).
*.jsonl

# Runtime / ferramentas
node_modules/
.DS_Store
*.swp
docs/research/

# Marcador interno do Claude Code (marketplace orfao) — nunca versionar
.orphaned_at
